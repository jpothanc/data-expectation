# Python 3.8+ compatible; tested on Python 3.12.
# Minimum versions are set to the earliest release that supports Python 3.12.

# ── Data processing ─────────────────────────────────────────────────────────
# pandas 2.0 is the first release to officially support Python 3.12.
# pandas 1.x uses datetime.datetime.utcnow() which is deprecated in 3.12
# and was removed in some internal paths, causing runtime warnings / errors.
pandas>=2.0.0

# Great Expectations 0.18.12+ added Python 3.12 support.
# Pin to the 0.18 series to keep the existing gx / gxe API intact;
# GE 1.x is a breaking-change release that would require validator rewrites.
great-expectations>=0.18.12,<1.0.0

# ── Configuration ───────────────────────────────────────────────────────────
pyyaml>=6.0.1

# ── Database support (optional — only needed for DatabaseDataLoader) ────────
pyodbc>=5.0.0
sqlalchemy>=2.0.0

# ── Web API ─────────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
flask-caching>=2.3.0
flasgger>=0.9.7.1

# ── Excel export ─────────────────────────────────────────────────────────────
openpyxl>=3.1.2

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
